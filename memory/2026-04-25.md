# Daily Notes - 2026-04-25

## Windows Build Attempt

### Key finding: Circular include chain from DX8Wrapper:public WW3DBackend
- The renderer abstraction work (Batches 1-15) caused a circular include chain:
  `dx8wrapper.h -> surfaceclass.h -> ww3dbackend.h -> meshrenderer.h -> mesh.h -> dx9polygonrenderer.h -> dx8wrapper.h`
- Tried many fixes: moving texture.h, forward declarations, #pragma once removal
- The chain couldn't be broken without reverting DX9 changes

### Resolution: Reset to pre-abstraction state
- Reset `develop` to commit `225838bb` (Merge PR #101, state before renderer abstraction batches)
- Force-pushed to origin
- `develop` now effectively has NO renderer abstraction work

### Current Windows build status
- `ww3d2`: Builds successfully ✅
- `renegade`: Fails at `atlbase.h` missing (VS ATL not installed)
  - Error in `Code/WWOnline/atlbase_compat.h:5`
  - This is a Visual Studio tooling issue, not a code issue

### Technical notes for future renderer abstraction work
- Composition (HAS-A) over inheritance (IS-A) for DX8Backend
- DX8Wrapper should NOT inherit WW3DBackend
- Instead: Create separate DX8Backend class that wraps DX8Wrapper
- Keep BackendTextureHandle/BackendSurfaceHandle in ww3dbackend.h only
- Never include ww3dbackend.h in files that are part of the circular chain
